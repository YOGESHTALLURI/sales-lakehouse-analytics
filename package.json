{
  "name": "sales-lakehouse-analytics",
  "version": "0.1.0",
  "private": true,
  "description": "Sales management application with an immutable data lake and DuckDB warehouse",
  "license": "MIT",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
